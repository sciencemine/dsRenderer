{{ce-player ce=ce oncedone=(action oncedone ce) isTeaser=false
        callbacks=(hash
                onpause=onpause
                onplay=onplay)
        options=(hash
                controls=true)}}